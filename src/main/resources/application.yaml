spring:
  application:
    name: rpc-gateway
  config:
    import: optional:file:.env[.properties]

ethereum:
  rpc:
    url: ${ETHEREUM_SEPOLIA_RPC_URL:}

cache:
  balance:
    ttl-seconds: ${BALANCE_CACHE_TTL_SECONDS}
